[Unit]
Description=FTP Service
Wants=network-online.target
After=network-online.target

[Service]
Type=notify
KillMode=none
Environment=GOMAXPROCS=2

ExecStart=/usr/bin/curlftpfs {{ ftp_server_host }} {{ ftp_mount_directory_path }} \
  -o user={{ ftp_server_userpass }},allow_other,ssl,no_verify_peer \
  -r \
  -f \
  --verbose

StandardOutput=file:{{ rclone_log_file_path }}
ExecStop=/bin/fusermount -uz {{ rclone_mount_directory_path }}
Restart=on-failure

[Install]
WantedBy=default.target
