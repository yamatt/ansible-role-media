server {
    listen 80;
    server_name {{ secrets.body.secrets.DOMAIN }};

    location /.well-known {
      root /var/www;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
